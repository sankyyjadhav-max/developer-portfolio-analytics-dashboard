1. Start server in `server`: npm install, copy .env.example .env, npx prisma generate, npx prisma migrate dev --name init, npm run dev.
2. Start client in `client`: npm install, copy .env.example .env.local, npm run dev.
3. Register, create/publish portfolio, add projects, then open `/p/<slug>` and visit `/dashboard/analytics`.
