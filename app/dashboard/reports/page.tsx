'use client';import {useState} from "react";import {API,token} from "../../../lib/api";
export default function Reports(){const [busy,setBusy]=useState(false);async function download(){setBusy(true);try{const r=await fetch(`${API}/api/reports/pdf`,{headers:{Authorization:`Bearer ${token()}`}});if(!r.ok)throw new Error("Report generation failed");const b=await r.blob(),u=URL.createObjectURL(b),a=document.createElement("a");a.href=u;a.download="portfolio-analytics.pdf";a.click();URL.revokeObjectURL(u)}finally{setBusy(false)}}return <><div className="top"><div><h1>Reports</h1><p className="muted">Generate a PDF from your real analytics data.</p></div></div><div className="card"><h3>Portfolio performance report</h3><p className="muted">Includes visitors, unique visitors, project clicks, traffic sources, devices and browsers for the default recent reporting period.</p><button className="btn primary" onClick={download} disabled={busy}>{busy?"Generating…":"Download PDF report"}</button></div></>}