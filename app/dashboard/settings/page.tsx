'use client';import {useEffect,useState} from "react";import {api} from "../../../lib/api";
export default function Settings(){const [p,setP]=useState<any>();useEffect(()=>{api("/api/portfolio").then(setP)},[]);return <><div className="top"><div><h1>Settings</h1><p className="muted">Manage your portfolio preferences.</p></div></div>{p&&<div className="card form"><label className="label">Public slug<input className="input" value={p.slug} onChange={e=>setP({...p,slug:e.target.value})}/></label><label className="label">Template<select className="input" value={p.template} onChange={e=>setP({...p,template:e.target.value})}><option value="modern">Modern</option><option value="creative">Creative</option></select></label><label className="label">Theme<select className="input" value={p.theme} onChange={e=>setP({...p,theme:e.target.value})}><option value="dark">Dark</option><option value="light">Light</option><option value="system">System</option></select></label><button className="btn primary" onClick={async()=>{await api("/api/portfolio",{method:"PUT",body:JSON.stringify(p)});alert("Settings saved")}}>Save settings</button></div>}</>}